import React from 'react'
import ReactDOM from 'react-dom/client'
import FaceTrackingApp from './index.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <FaceTrackingApp />
  </React.StrictMode>,
)
